Circular icon-only button for toolbars and headers (back, share, notifications).

```jsx
<IconButton active><HeartIcon/></IconButton>
```
`size` in px (default 40). `active` swaps to accent tint, used for toggled state (e.g. liked, filter selected).
