Filter/category pill. `neutral` tone toggles selected (coral fill) for feed filters like "Mis clubes". `gold` tone is a static label for editorial categories (Guías, Autores, Cursos).

```jsx
<Chip selected onClick={...}>Mis clubes</Chip>
<Chip tone="gold">Guía</Chip>
```
