Wraps a Lucide icon (loaded from CDN — no in-house icon set exists yet). Requires `<script src="https://unpkg.com/lucide@latest"></script>` on the host page.

```jsx
<Icon name="heart" size={18} color="var(--accent-500)"/>
```
