import React from 'react';
export interface IconProps {
  /** Lucide icon name, e.g. "heart", "play", "mic", "chevron-down" */
  name: string;
  size?: number;
  color?: string;
  strokeWidth?: number;
}
