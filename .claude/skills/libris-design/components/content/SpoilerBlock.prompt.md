Collapsed spoiler wrapper for the comment feed — click to reveal the wrapped comment/quote/voice note.

```jsx
<SpoilerBlock><p>Al final, el faro se derrumba.</p></SpoilerBlock>
```
Uses the shared `Icon` (`eye-off`) — no emoji.
